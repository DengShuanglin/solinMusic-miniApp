<view class="video">
  <view class="item" wx:for="{{mvList}}" wx:key="id">
    <video-list-item 
      item="{{item}}" 
      bindtap="handleVideoItemClick"
      data-item="{{item}}">
    </video-list-item>
  </view>
</view>